{
  "name": "tf2-web",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT-0",
  "private": true,
  "dependencies": {
    "express": "^5.2.1",
    "nunjucks": "^3.2.4",
    "pino-http": "^11.0.0"
  },
  "devDependencies": {
    "pino-dev": "^4.0.3",
    "sass": "^1.101.0"
  },
  "scripts": {
    "start": "node server.js",
    "watch": "node --watch --watch-path=./views --watch-path=./routes server.js | pino-dev",
    "build:css": "sass assets --pkg-importer=node",
    "watch:css": "sass --watch assets"
  }
}
